$ git init
$ git remote add origin https://github.com/NKALLU/manhan.git
$ git pull origin master
git status
git add GitNotes.txt
git status
git commit  -m "Adding new files called GitNotes.txt"
git add -A  == add all the files

git commit -a -m "Adding new files"
git log

git remote show origin